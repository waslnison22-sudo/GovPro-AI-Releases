name: Actions sanity check

on:
  push:
    paths:
      - "ACTIONS_SANITY_TRIGGER.txt"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "ACTIONS_SANITY_OK"
